# Relative URL in the browser; Vite dev server proxies /api → backend
VITE_API_URL=/api

# MUST match backend PORT (see backend/.env). Default in vite.config is :5000.
# If PORT=5001:
VITE_PROXY_TARGET=http://127.0.0.1:5001
# If PORT=5000, omit or use:
# VITE_PROXY_TARGET=http://127.0.0.1:5000
